# Test Configuration for ChemVantage
# This configuration is used during unit testing

# Logging
logging.level.org.chemvantage=DEBUG
logging.level.org.springframework=INFO

# Disable Spring Boot banner during tests
spring.main.banner-mode=off

# Test environment marker
test.mode=true
